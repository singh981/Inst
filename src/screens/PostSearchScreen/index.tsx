import {Text, View} from 'react-native';

const PostSearchScreen = () => {
    return (
        <View
            style={{
                flex: 1,
                justifyContent: 'center',
                alignItems: 'center',
            }}>
            <Text>Post Search Screen</Text>
        </View>
    );
};

export default PostSearchScreen;
